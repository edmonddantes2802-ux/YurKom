/**
 * Ограничения на пользовательский ввод.
 *
 * Отдельный модуль без зависимостей: значения нужны и на сервере (валидация
 * в `/api/lead`), и в клиентской форме. Если бы они лежали в
 * `lib/validation.ts`, форма потянула бы в браузерный бандл ещё и
 * libphonenumber-js.
 */

/** Короче — это не описание ситуации, а «помогите». */
export const SITUATION_MIN = 10;

/**
 * Длиннее человек в форме не пишет, а Telegram всё равно режет сообщение по
 * своему лимиту. Лишнее обрезается с пометкой, а не теряется молча.
 */
export const SITUATION_MAX = 2000;

/** Ввод телефона: с пробелами, скобками и дефисами. Нормализуется на сервере. */
export const PHONE_INPUT_MAX = 32;

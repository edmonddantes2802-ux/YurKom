/**
 * Базовый URL сайта — единая точка правды для sitemap, robots, канонических
 * ссылок и JSON-LD. Фолбэк указывает на боевой домен, поэтому даже сборка
 * без заданной env даёт корректные абсолютные адреса, а не example.com.
 */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || 'https://mitragost.ru';

/**
 * Картинка для соцсетей. Метаданные в Next мержатся поверхностно: `openGraph`,
 * заданный на странице, целиком перекрывает родительский. Поэтому изображение
 * подставляется в каждую страницу из этой константы, а не из layout.
 */
export const OG_IMAGE = {
  url: '/og-image.png',
  width: 1200,
  height: 630,
  alt: 'Митрагост — юридическая антикризисная служба',
};

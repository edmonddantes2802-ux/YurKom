# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env — под секреты (токен бота, chat_id). Игнорируем всё, кроме шаблона:
# прежний список не покрывал .env.production и .env.development, а положить
# туда боевой токен — вопрос одной невнимательной команды.
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts
